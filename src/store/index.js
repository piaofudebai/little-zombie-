import { createStore } from 'vuex'

//* vue2.0 创建仓库 newVuex.Store({})
//* vue3.0 创建仓库 createStore({})
export default createStore({
  state: {
    username: 'ls'
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
